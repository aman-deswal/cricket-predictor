import { ScreenState } from '@/components/screen-state';

export default function HistoryScreen() {
  return (
    <ScreenState
      title="History foundation ready"
      detail="Prediction outcomes will land here in the History parity slice tracked by issue #182."
    />
  );
}
