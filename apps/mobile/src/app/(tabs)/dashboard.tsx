import { ScreenState } from '@/components/screen-state';

export default function DashboardScreen() {
  return (
    <ScreenState
      title="Dashboard foundation ready"
      detail="Accuracy and calibration metrics will land here in the Dashboard parity slice tracked by issue #182."
    />
  );
}
